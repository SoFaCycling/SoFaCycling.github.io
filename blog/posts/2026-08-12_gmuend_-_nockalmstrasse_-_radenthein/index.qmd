---
title: "Gmünd - Nockalmstraße - Radenthein"
date: "2026-08-12"
description: "118 km mit 2508 Hm"
image: thumbnail.png
resources:
  - track.json
  - img/**

draft: false
trip: Sommerurlaub 2026
categories: [Sommerurlaub 2026, Österreich]
strava_id: 19712291899
distance_km: 118.2
elevation_m: 2508
moving_time_min: 373

format: 
  html:
    toc: false

    include-in-header:
      text: |
        <link rel="stylesheet" href="https://unpkg.com/leaflet/dist/leaflet.css"/>
        <script src="https://unpkg.com/leaflet/dist/leaflet.js"></script>
        <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

lightbox:
  match: auto
---


::: {.stats-grid-container}
::: {.stats-grid}

::: {.grid-item}
{{< bi arrow-right-circle class="param-icon">}}
<span class="stats-value">118.2</span>
<span class="stats-label">Kilometer</span>
:::

::: {.grid-item}
{{< bi arrow-up-right-circle class="param-icon">}}
<span class="stats-value">2508</span>
<span class="stats-label">Höhenmeter</span>
:::

::: {.grid-item}
{{< bi clock-history class="param-icon">}}
<span class="stats-value">6h 13min</span>
<span class="stats-label">Fahrzeit</span>
:::

::: {.grid-item}
{{< bi speedometer class="param-icon">}}
<span class="stats-value">19.0</span>
<span class="stats-label">km/h (66.3 max.)</span>
:::

::: {.grid-item}
{{< bi lightning class="param-icon">}}
<span class="stats-value">93.8</span>
<span class="stats-label">Watt (342 max.)</span>
:::

::: {.grid-item}
{{< bi heart-pulse class="param-icon">}}
<span class="stats-value">123.0</span>
<span class="stats-label">bpm (176.0 max.)</span>
:::

:::
:::


&nbsp;


### Unser erster Österreicher 2K

Der erste 2000er in Österreich (von insgesamt nur drei) — und zudem der östlichste der Alpen überhaupt — ist eingetütet: Die Nockalmstraße mit ihren höchsten Punkten Eisentalhöhe auf 2042 m und Schiestlscharte auf 2024 m. 

Wir wählten eine wirklich herrliche Runde über Gmünd und fuhren die Mautstraße aus Nordwest von Innerkrems Richtung Südost nach Ebene Reichenau. Der Rückweg nach Spittal an der Drau führte dann entsprechend über Patergassen, Bad Kleinkirchheim und Radenthein.

Die Nockalmstraße liegt im UNESCO Biosphärenpark Kärntner Nockberge. Namensgeber sind die sogenannten Nocken — grasbewachsene Berge mit runden Kuppen. Ein Unikat im gesamten Alpenraum!

Die Nockalmstraße ist zudem ein reines Serpentinen-Paradies. Insgesamt wartet sie mit 52 Kehren — oder wie es hier genannt wird: Reidn — auf. Da war der Spaß vorprogrammiert.

Eine rundum gelungene Ausfahrt: kurzweilig, höhenmeterreich und landschaftlich eine Wucht.


&nbsp;



<div id="map" style="height: 400px;"></div>

<script>
fetch(new URL("track.json", document.baseURI))
  .then(response => response.json())
  .then(data => {

    const latlngs = data.latlng.map(coord => [coord[0], coord[1]]);

    const map = L.map('map');
    const polyline = L.polyline(latlngs, { color: 'black', weight: 2 }).addTo(map); // <-- schwarz, dicke 3px

    map.fitBounds(polyline.getBounds());

    L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
      attribution: '&copy; OpenStreetMap contributors'
    }).addTo(map);

  });
</script>




<div style="height: 250px; width: 100%; position: relative; margin-top: 20px;">
  <canvas id="elevationChart"></canvas>
</div>

<script>
fetch(new URL("track.json", document.baseURI))
  .then(response => response.json())
  .then(data => {
    
    // 1. Rolling Mean (Moving Average, k=100)
    const k = 100;
    const half_k = Math.floor(k / 2);
    const smoothed_alt = data.altitude.map((val, i, arr) => {
      let start = Math.max(0, i - half_k);
      let end = Math.min(arr.length, i + half_k);
      let subset = arr.slice(start, end);
      let sum = subset.reduce((a, b) => a + b, 0);
      return sum / subset.length;
    });

    // 2. Y-Minimum berechnen und als VOLLE Zahl abrunden (Behebt Problem 1)
    const min_alt = Math.min(...data.altitude);
    const lim_y = Math.floor(min_alt * 0.9);

    // 3. Maximale Distanz ermitteln
    const maxDist = data.distance[data.distance.length - 1];

    // 4. Datenstruktur für lineare Achse umbauen (X/Y-Paare)
    const chartData = data.distance.map((dist, i) => ({
      x: dist,
      y: smoothed_alt[i]
    }));

    const ctx = document.getElementById('elevationChart').getContext('2d');
    new Chart(ctx, {
      type: 'line',
      data: {
        datasets: [{
          data: chartData,
          borderColor: 'black',
          borderWidth: 1.5,
          backgroundColor: 'rgba(204, 204, 204, 0.3)',
          fill: 'start',
          pointRadius: 0,
          pointHoverRadius: 5,
          tension: 0
        }]
      },
      options: {
        responsive: true,
        maintainAspectRatio: false,
        plugins: {
          legend: { display: false },
          tooltip: {
            displayColors: false,
            callbacks: {
              // Tooltip greift jetzt auf die numerischen Daten zu
              title: (items) => items[0].parsed.x.toFixed(1) + ' km',
              label: (item) => Math.round(item.parsed.y) + ' hm'
            }
          }
        },
        scales: {
          x: {
            type: 'linear',  // Behebt Problem 2b (gleichmäßige Abschnitte)
            position: 'bottom',
            min: 0,
            max: maxDist,
            title: { display: true, text: 'km', color: '#666' },
            grid: { display: false },
            ticks: {
              maxTicksLimit: 10,
              callback: function(value, index, values) {
                // Wenn es der letzte Wert (Ende der Tour) ist, zeige 1 Nachkommastelle
                if (value === maxDist) {
                  return value.toFixed(0);
                }
                // Sonst zeige absolut saubere, volle Kilometer-Schritte
                return Math.round(value);
              }
            },
            afterBuildTicks: function(scale) {
              // Behebt Problem 2a: Erzwingt das Ende der Tour als letzten Tick
              if (scale.ticks.length > 0) {
                let lastTick = scale.ticks[scale.ticks.length - 1].value;
                // Verhindert, dass der letzte automatische Tick zu nah am Ende klebt und überlappt
                if (maxDist - lastTick < (maxDist * 0.08)) {
                  scale.ticks.pop(); 
                }
              }
              scale.ticks.push({ value: maxDist });
            }
          },
          y: {
            min: lim_y,
            title: { display: true, text: 'Höhenmeter', color: '#666' },
            grid: { color: 'rgba(0, 0, 0, 0.05)' },
            ticks: {
              precision: 0 // Erzwingt zusätzlich nur volle Höhenmeter auf der Achse
            }
          }
        }
      }
    });
  });
</script>



::: {.gallery}

![](img/20260812_092613.webp){group="tour"}

![](img/20260812_111343.webp){group="tour"}

![](img/20260812_112629.webp){group="tour"}

![](img/20260812_114042.webp){group="tour"}

![](img/20260812_120125.webp){group="tour"}

![](img/20260812_120556.webp){group="tour"}

![](img/20260812_121327.webp){group="tour"}

![](img/20260812_121427.webp){group="tour"}

![](img/20260812_121809.webp){group="tour"}

![](img/20260812_122059.webp){group="tour"}

![](img/20260812_123204.webp){group="tour"}

![](img/20260812_123802.webp){group="tour"}

![](img/20260812_131927.webp){group="tour"}

![](img/20260812_132952.webp){group="tour"}

![](img/20260812_133242.webp){group="tour"}

![](img/20260812_142053.webp){group="tour"}

![](img/20260812_151427.webp){group="tour"}

![](img/20260812_152246.webp){group="tour"}

:::


&nbsp;

::: {.trip-dashboard}

### Sommerurlaub 2026: Stand aktuell

<div class="trip-grid">

<div class="trip-card">
<div class="trip-value">2289</div>
<div class="trip-label">Kilometer</div>
</div>

<div class="trip-card">
<div class="trip-value">64018</div>
<div class="trip-label">Höhenmeter</div>
</div>

<div class="trip-card">
<div class="trip-value">138h 49min</div>
<div class="trip-label">Fahrzeit</div>
</div>

</div>

:::
