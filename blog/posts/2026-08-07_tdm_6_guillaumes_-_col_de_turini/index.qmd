---
title: "TdM #6: Guillaumes - Col de Turini"
date: "2026-08-07"
description: "102 km mit 3322 Hm"
image: thumbnail.png
resources:
  - track.json
  - img/**

draft: false
trip: Sommerurlaub 2026
categories: [Sommerurlaub 2026, Frankreich, Tour de Mariage]
strava_id: 19643408800
distance_km: 102.1
elevation_m: 3322
moving_time_min: 411

format: 
  html:
    toc: false

    include-in-header:
      text: |
        <link rel="stylesheet" href="https://unpkg.com/leaflet/dist/leaflet.css"/>
        <script src="https://unpkg.com/leaflet/dist/leaflet.js"></script>
        <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

lightbox:
  match: auto
---


::: {.stats-grid-container}
::: {.stats-grid}

::: {.grid-item}
{{< bi arrow-right-circle class="param-icon">}}
<span class="stats-value">102.1</span>
<span class="stats-label">Kilometer</span>
:::

::: {.grid-item}
{{< bi arrow-up-right-circle class="param-icon">}}
<span class="stats-value">3322</span>
<span class="stats-label">Höhenmeter</span>
:::

::: {.grid-item}
{{< bi clock-history class="param-icon">}}
<span class="stats-value">6h 51min</span>
<span class="stats-label">Fahrzeit</span>
:::

::: {.grid-item}
{{< bi speedometer class="param-icon">}}
<span class="stats-value">14.9</span>
<span class="stats-label">km/h (62.9 max.)</span>
:::

::: {.grid-item}
{{< bi lightning class="param-icon">}}
<span class="stats-value">103.2</span>
<span class="stats-label">Watt (340 max.)</span>
:::

::: {.grid-item}
{{< bi heart-pulse class="param-icon">}}
<span class="stats-value">131.2</span>
<span class="stats-label">bpm (173.0 max.)</span>
:::

:::
:::


&nbsp;


### Tag der Extreme: Hitze, Regenschauer und Bergankunft am Gruselhotel

Wir haben es geschafft, wir sind in uns sehr bekannten Gefilden angekommen — auf dem Gipfel des Col de Turini auf 1607 m. Damit gab es heute die erste und einzige Bergankunft der Tour de Mariage. 

Der Start in Guillaumes war überaus gelungen: Wir kurbelten zunächst gemütlich nach einem leckeren Frühstück den herrlichen Col de Valberg (1673 m) hinauf und fuhren dann weiter über den etwas bekannteren Col de la Couillole (1678 m), um dann den Col Saint-Martin (1500 m) in Angriff zu nehmen. Spätestens hier lagen die Nerven erstmals blank; die Auffahrt war zwar landschaftlich insbesondere zu Beginn ganz wunderbar, aber auch durchweg sehr exponiert. Und das bedeutete Schwitzen bei ca. 36°C in der prallen Sonne. Die Abfahrt Richtung Col de Turini fühlte sich erst an, als ob man einen riesigen, viel zu heiß eingestellten Fön auf uns gerichtet hätte. Wir waren so happy, als ein paar kleine Wölkchen aufzogen.

Leider hielt die Freude nicht lang an. Machen wir es kurz: Das Ende vom Lied war, dass wir in einem heftigen Regenguss erst 20 Kilometer bergab und dann nochmals klatschnass 15 Kilometer mit netten 1100 Höhenmeter bergauf radeln mussten. Was dann noch zur Perfektion fehlte, ist die Ankunft in einem Hotel, dass buchstäblich auseinanderfällt... Genau. 

Diese Chaos-Tour der Extreme ist nun Geschichte. Wir sind trotzdem stolz, dass wir eisern an unserem Plan festgehalten und durchgezogen haben. Glücklicherweise haben wir ja die ganze Nacht Zeit, uns zu feiern. Denn schlafen dürfte in dieser Bruchbude hier schwierig werden.

Morgen sehen wir dann endlich unser geliebtes Nizza wieder! Aber auch noch nicht auf direktem Wege, sondern über unsere Lieblings-Cols in der Region. Wir sind gespannt — nicht nur, aber auch auf die Zieleinfahrt der Radlerinnen der Tour de France Femmes in Nizza.


&nbsp;



<div id="map" style="height: 400px;"></div>

<script>
fetch(new URL("track.json", document.baseURI))
  .then(response => response.json())
  .then(data => {

    const latlngs = data.latlng.map(coord => [coord[0], coord[1]]);

    const map = L.map('map');
    const polyline = L.polyline(latlngs, { color: 'black', weight: 2 }).addTo(map); // <-- schwarz, dicke 3px

    map.fitBounds(polyline.getBounds());

    L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
      attribution: '&copy; OpenStreetMap contributors'
    }).addTo(map);

  });
</script>




<div style="height: 250px; width: 100%; position: relative; margin-top: 20px;">
  <canvas id="elevationChart"></canvas>
</div>

<script>
fetch(new URL("track.json", document.baseURI))
  .then(response => response.json())
  .then(data => {
    
    // 1. Rolling Mean (Moving Average, k=100)
    const k = 100;
    const half_k = Math.floor(k / 2);
    const smoothed_alt = data.altitude.map((val, i, arr) => {
      let start = Math.max(0, i - half_k);
      let end = Math.min(arr.length, i + half_k);
      let subset = arr.slice(start, end);
      let sum = subset.reduce((a, b) => a + b, 0);
      return sum / subset.length;
    });

    // 2. Y-Minimum berechnen und als VOLLE Zahl abrunden (Behebt Problem 1)
    const min_alt = Math.min(...data.altitude);
    const lim_y = Math.floor(min_alt * 0.9);

    // 3. Maximale Distanz ermitteln
    const maxDist = data.distance[data.distance.length - 1];

    // 4. Datenstruktur für lineare Achse umbauen (X/Y-Paare)
    const chartData = data.distance.map((dist, i) => ({
      x: dist,
      y: smoothed_alt[i]
    }));

    const ctx = document.getElementById('elevationChart').getContext('2d');
    new Chart(ctx, {
      type: 'line',
      data: {
        datasets: [{
          data: chartData,
          borderColor: 'black',
          borderWidth: 1.5,
          backgroundColor: 'rgba(204, 204, 204, 0.3)',
          fill: 'start',
          pointRadius: 0,
          pointHoverRadius: 5,
          tension: 0
        }]
      },
      options: {
        responsive: true,
        maintainAspectRatio: false,
        plugins: {
          legend: { display: false },
          tooltip: {
            displayColors: false,
            callbacks: {
              // Tooltip greift jetzt auf die numerischen Daten zu
              title: (items) => items[0].parsed.x.toFixed(1) + ' km',
              label: (item) => Math.round(item.parsed.y) + ' hm'
            }
          }
        },
        scales: {
          x: {
            type: 'linear',  // Behebt Problem 2b (gleichmäßige Abschnitte)
            position: 'bottom',
            min: 0,
            max: maxDist,
            title: { display: true, text: 'km', color: '#666' },
            grid: { display: false },
            ticks: {
              maxTicksLimit: 10,
              callback: function(value, index, values) {
                // Wenn es der letzte Wert (Ende der Tour) ist, zeige 1 Nachkommastelle
                if (value === maxDist) {
                  return value.toFixed(0);
                }
                // Sonst zeige absolut saubere, volle Kilometer-Schritte
                return Math.round(value);
              }
            },
            afterBuildTicks: function(scale) {
              // Behebt Problem 2a: Erzwingt das Ende der Tour als letzten Tick
              if (scale.ticks.length > 0) {
                let lastTick = scale.ticks[scale.ticks.length - 1].value;
                // Verhindert, dass der letzte automatische Tick zu nah am Ende klebt und überlappt
                if (maxDist - lastTick < (maxDist * 0.08)) {
                  scale.ticks.pop(); 
                }
              }
              scale.ticks.push({ value: maxDist });
            }
          },
          y: {
            min: lim_y,
            title: { display: true, text: 'Höhenmeter', color: '#666' },
            grid: { color: 'rgba(0, 0, 0, 0.05)' },
            ticks: {
              precision: 0 // Erzwingt zusätzlich nur volle Höhenmeter auf der Achse
            }
          }
        }
      }
    });
  });
</script>



::: {.gallery}

![](img/20260807_084959.webp){group="tour"}

![](img/20260807_090349.webp){group="tour"}

![](img/20260807_113817.webp){group="tour"}

![](img/20260807_120254.webp){group="tour"}

![](img/20260807_123304.webp){group="tour"}

![](img/20260807_123428.webp){group="tour"}

![](img/20260807_124656.webp){group="tour"}

![](img/20260807_152624.webp){group="tour"}

![](img/20260807_174625.webp){group="tour"}

![](img/20260807_185809.webp){group="tour"}

:::


&nbsp;

::: {.trip-dashboard}

### Sommerurlaub 2026: Stand aktuell

<div class="trip-grid">

<div class="trip-card">
<div class="trip-value">1934</div>
<div class="trip-label">Kilometer</div>
</div>

<div class="trip-card">
<div class="trip-value">56836</div>
<div class="trip-label">Höhenmeter</div>
</div>

<div class="trip-card">
<div class="trip-value">119h 41min</div>
<div class="trip-label">Fahrzeit</div>
</div>

</div>

:::
