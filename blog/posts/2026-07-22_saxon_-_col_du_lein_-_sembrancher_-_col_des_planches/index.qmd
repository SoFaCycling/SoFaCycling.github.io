---
title: "Saxon - Col du Lein - Sembrancher - Col des Planches"
date: "2026-07-22"
description: "82 km mit 2373 Hm"
image: thumbnail.png
resources:
  - track.json
  - img/**

draft: false
trip: Sommerurlaub 2026
categories: [Sommerurlaub 2026, Schweiz]
strava_id: 19417326380
distance_km: 82.3
elevation_m: 2373
moving_time_min: 299

format: 
  html:
    toc: false

    include-in-header:
      text: |
        <link rel="stylesheet" href="https://unpkg.com/leaflet/dist/leaflet.css"/>
        <script src="https://unpkg.com/leaflet/dist/leaflet.js"></script>
        <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

lightbox:
  match: auto
---


::: {.stats-grid-container}
::: {.stats-grid}

::: {.grid-item}
{{< bi arrow-right-circle class="param-icon">}}
<span class="stats-value">82.3</span>
<span class="stats-label">Kilometer</span>
:::

::: {.grid-item}
{{< bi arrow-up-right-circle class="param-icon">}}
<span class="stats-value">2373</span>
<span class="stats-label">Höhenmeter</span>
:::

::: {.grid-item}
{{< bi clock-history class="param-icon">}}
<span class="stats-value">4h 59min</span>
<span class="stats-label">Fahrzeit</span>
:::

::: {.grid-item}
{{< bi speedometer class="param-icon">}}
<span class="stats-value">16.5</span>
<span class="stats-label">km/h (55.9 max.)</span>
:::

::: {.grid-item}
{{< bi lightning class="param-icon">}}
<span class="stats-value">99.2</span>
<span class="stats-label">Watt (232 max.)</span>
:::

::: {.grid-item}
{{< bi heart-pulse class="param-icon">}}
<span class="stats-value">130.3</span>
<span class="stats-label">bpm (172.0 max.)</span>
:::

:::
:::


&nbsp;


### Lein-Double zwischen Obstplantagen und Ziegen-Gehege

Der Startschuss in den ersten Anstieg des Tages fiel für uns heute in Saxon. Hier hing es zunächst vorbei an wunderschönen, prall gefüllten Obstplantagen – von Aprikosen über Birnen und Pflaumen bis hin zu Äpfeln war so ziemlich alles dabei, was uns das Wasser im Mund zusammenlaufen ließ. Aber wir haben natürlich nichts stibitzt (auch wenn das bei diesem Anblick schwer fiel)!

Dafür ging es direkt zur Sache: Zuerst hoch auf den Col du Lein und auf der anderen Seite hinunter Richtung Sembrancher. Um von dort aus auf den Col des Planches zu gelangen, hieß es dann direkt wieder Kurbeln – und zwar gegen heftige und fönartig-heiße Böen, die gefühlt von allen Seiten pusteten. Selbst oben auf über 1600m war an Abkühlung kaum zu denken. Die Hitze hatte uns voll im Griff, mit teilweise wieder deutlich über 30°C

Der Weg zurück nach Saxon führte uns zwangsläufig wieder über den Lein. Dementsprechend gab es die kürzere Lein-Auffahrt mit rund 300 zusätzlichen Höhenmeter sogleich als Zugabe obendrauf. Das bedeutete auch, ein zweites Mal den Schotterparcours um den Gipfel des Lein zu absolvieren. Glücklicherweise ließ sich dieser relativ gut mit dem Rennrad fahren und Stürze blieben aus. 

Es war eine wirklich schöne, aber bei den Temperaturen auch ordentlich schweißtreibende Runde, die wir aber recht effektiv durchpeitschten. Am Ende waren wir so zeitig zurück (zugegeben: auch dank des Rückenwindes auf der Transitstrecke durchs Tal), dass sogar noch ein kurzer Abstecher zum "Ziegen-Streichelzoo" vorm hiesigen Bioladen drin war.


&nbsp;



<div id="map" style="height: 400px;"></div>

<script>
fetch(new URL("track.json", document.baseURI))
  .then(response => response.json())
  .then(data => {

    const latlngs = data.latlng.map(coord => [coord[0], coord[1]]);

    const map = L.map('map');
    const polyline = L.polyline(latlngs, { color: 'black', weight: 2 }).addTo(map); // <-- schwarz, dicke 3px

    map.fitBounds(polyline.getBounds());

    L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
      attribution: '&copy; OpenStreetMap contributors'
    }).addTo(map);

  });
</script>




<div style="height: 250px; width: 100%; position: relative; margin-top: 20px;">
  <canvas id="elevationChart"></canvas>
</div>

<script>
fetch(new URL("track.json", document.baseURI))
  .then(response => response.json())
  .then(data => {
    
    // 1. Rolling Mean (Moving Average, k=100)
    const k = 100;
    const half_k = Math.floor(k / 2);
    const smoothed_alt = data.altitude.map((val, i, arr) => {
      let start = Math.max(0, i - half_k);
      let end = Math.min(arr.length, i + half_k);
      let subset = arr.slice(start, end);
      let sum = subset.reduce((a, b) => a + b, 0);
      return sum / subset.length;
    });

    // 2. Y-Minimum berechnen und als VOLLE Zahl abrunden (Behebt Problem 1)
    const min_alt = Math.min(...data.altitude);
    const lim_y = Math.floor(min_alt * 0.9);

    // 3. Maximale Distanz ermitteln
    const maxDist = data.distance[data.distance.length - 1];

    // 4. Datenstruktur für lineare Achse umbauen (X/Y-Paare)
    const chartData = data.distance.map((dist, i) => ({
      x: dist,
      y: smoothed_alt[i]
    }));

    const ctx = document.getElementById('elevationChart').getContext('2d');
    new Chart(ctx, {
      type: 'line',
      data: {
        datasets: [{
          data: chartData,
          borderColor: 'black',
          borderWidth: 1.5,
          backgroundColor: 'rgba(204, 204, 204, 0.3)',
          fill: 'start',
          pointRadius: 0,
          pointHoverRadius: 5,
          tension: 0
        }]
      },
      options: {
        responsive: true,
        maintainAspectRatio: false,
        plugins: {
          legend: { display: false },
          tooltip: {
            displayColors: false,
            callbacks: {
              // Tooltip greift jetzt auf die numerischen Daten zu
              title: (items) => items[0].parsed.x.toFixed(1) + ' km',
              label: (item) => Math.round(item.parsed.y) + ' hm'
            }
          }
        },
        scales: {
          x: {
            type: 'linear',  // Behebt Problem 2b (gleichmäßige Abschnitte)
            position: 'bottom',
            min: 0,
            max: maxDist,
            title: { display: true, text: 'km', color: '#666' },
            grid: { display: false },
            ticks: {
              maxTicksLimit: 10,
              callback: function(value, index, values) {
                // Wenn es der letzte Wert (Ende der Tour) ist, zeige 1 Nachkommastelle
                if (value === maxDist) {
                  return value.toFixed(0);
                }
                // Sonst zeige absolut saubere, volle Kilometer-Schritte
                return Math.round(value);
              }
            },
            afterBuildTicks: function(scale) {
              // Behebt Problem 2a: Erzwingt das Ende der Tour als letzten Tick
              if (scale.ticks.length > 0) {
                let lastTick = scale.ticks[scale.ticks.length - 1].value;
                // Verhindert, dass der letzte automatische Tick zu nah am Ende klebt und überlappt
                if (maxDist - lastTick < (maxDist * 0.08)) {
                  scale.ticks.pop(); 
                }
              }
              scale.ticks.push({ value: maxDist });
            }
          },
          y: {
            min: lim_y,
            title: { display: true, text: 'Höhenmeter', color: '#666' },
            grid: { color: 'rgba(0, 0, 0, 0.05)' },
            ticks: {
              precision: 0 // Erzwingt zusätzlich nur volle Höhenmeter auf der Achse
            }
          }
        }
      }
    });
  });
</script>



::: {.gallery}

![](img/20260722_093620.webp){group="tour"}

![](img/20260722_093623.webp){group="tour"}

![](img/20260722_104149.webp){group="tour"}

![](img/20260722_104235.webp){group="tour"}

![](img/20260722_105948.webp){group="tour"}

![](img/20260722_112149.webp){group="tour"}

![](img/20260722_113127.webp){group="tour"}

![](img/20260722_123813.webp){group="tour"}

![](img/20260722_125548.webp){group="tour"}

![](img/20260722_125944.webp){group="tour"}

![](img/20260722_164254.webp){group="tour"}

:::


&nbsp;

::: {.trip-dashboard}

### Sommerurlaub 2026: Stand aktuell

<div class="trip-grid">

<div class="trip-card">
<div class="trip-value">725</div>
<div class="trip-label">Kilometer</div>
</div>

<div class="trip-card">
<div class="trip-value">21291</div>
<div class="trip-label">Höhenmeter</div>
</div>

<div class="trip-card">
<div class="trip-value">44h 0min</div>
<div class="trip-label">Fahrzeit</div>
</div>

</div>

:::
